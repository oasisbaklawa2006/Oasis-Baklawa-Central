# Claude Instructions — Oasis Baklawa Central / Mission Control

The programme is governed by the Appverse Mission Control authority and its machine-readable state. Preserve the assigned ASM work item, repository boundary, dependencies, and stop condition for every material task.

Central owns the internal Admin Web / Operational Control Tower. Do not create or edit Appverse Supabase migration SQL here and do not absorb Core, Buyer App, AI Studio, or Trace work without explicit Mission Control reassignment.

For mismatched pasted instructions respond `ROUTING REJECTED — instruction does not belong to this thread.`, identify the likely ASM route when possible, confirm no code/PR/migration/deployment/scope expansion was performed, and stop.

If an upstream dependency or gate is missing, mark the task blocked and stop at the safe boundary. Do not invent schema, data, identifiers, bypasses, or replacement authority.

Cross-scope defects may be evidenced minimally, but must return to Mission Control for reassignment rather than being fixed opportunistically.

`PR MERGED != STAGE CLEARED`. Report the precise gate state; programme completion, not PR completion, is the objective.
