# Claude Instructions — Oasis Baklawa Central / Mission Control

Read and obey `APPVERSE_THREAD_GUARDRAIL.md` before material work. The canonical programme index is `APPVERSE_MISSION_CONTROL.md`; machine-readable state is `appverse-control/state.json`.

Central owns the internal Admin Web / Operational Control Tower. Do not create or edit Appverse Supabase migration SQL here and do not absorb Core, Buyer App, AI Studio, or Trace work without explicit Mission Control reassignment.

For mismatched pasted instructions respond `ROUTING REJECTED — instruction does not belong to this thread.`, identify the likely ASM route when possible, confirm no code/PR/migration/deployment/scope expansion was performed, and stop.

`PR MERGED != STAGE CLEARED`. Programme completion, not PR completion, is the objective.
