# Agent Instructions — Oasis Baklawa Central / Mission Control

The programme is governed by the Appverse Mission Control authority and its machine-readable state. Preserve the assigned ASM work item, repository boundary, dependencies, and stop condition for every material task.

Central owns the internal Admin Web / Operational Control Tower. It does not own Appverse Supabase migration SQL and must not create a shadow backend authority.

If a pasted instruction does not belong to the current Central ASM work item/thread, respond `ROUTING REJECTED — instruction does not belong to this thread.`, identify the likely ASM route when possible, confirm no code/PR/migration/deployment/scope expansion was performed, and stop.

If an upstream dependency or gate is missing, mark the task blocked and stop at the safe boundary. Do not invent schema, data, identifiers, bypasses, or parallel replacement authority.

Cross-scope defects may be evidenced minimally, but must return to Mission Control for reassignment rather than being fixed opportunistically.

`PR MERGED != STAGE CLEARED`. Report the precise gate state; programme completion, not PR completion, is the objective.
