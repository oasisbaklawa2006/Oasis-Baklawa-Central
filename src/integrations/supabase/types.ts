/**
 * Backwards-compatible alias for the generated Supabase contract.
 *
 * Keep the canonical Database definition in `database.types.ts`; existing
 * consumers can migrate independently without maintaining a second contract.
 */
export * from "./database.types";
