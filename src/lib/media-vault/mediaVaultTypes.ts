/** Minimal media vault types for document graph (full vault UI may ship separately). */
export type MediaDocumentKind =
  | "invoice_document"
  | "payment_receipt"
  | "packing_photo"
  | "dispatch_proof"
  | "label_preview"
  | "whatsapp_attachment_hint"
  | "customer_reference_image"
  | "support_ticket_media";
