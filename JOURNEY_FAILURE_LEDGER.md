# JOURNEY FAILURE LEDGER

Consolidated, deduplicated list of every BROKEN / confirmed-defect finding across the four journey research passes (Golden Order Pipeline, B2B Customer, Internal Roles, Support). UNPROVEN items with no evidence either way are listed in the source matrices, not repeated here as defects — this ledger is only for items with a concrete, cited problem.

Severity legend: **P0** silent loss/security/data corruption · **P1** journey cannot complete · **P2** journey completes incorrectly / needs manual workaround · **P3** usability/observability/polish.

| FAIL-ID | Journey | Severity | Finding | Exact evidence | Owning repo |
|---|---|---|---|---|---|
| **FL-SUP-01** | D (Support) | **P0** | Support ticket queue RLS gates on literal `role IN ('admin','super_admin')`, not `is_internal_staff()`. The role Central's own routing designates as queue owner (`support_executive`) matches neither this policy nor the customer policies → **zero rows returned**. UI is reachable, queue silently empty. | `support_tickets_admin_all` RLS policy, Core baseline `20260723161256_legacy_role_authority_baseline.sql`; contradicted by `auth-routing.ts`/`roleAccess.ts`/`queueOwnership.ts` in Central | Core (RLS fix) |
| **FL-SUP-02** | D (Support) | **P0** | `AdminSupport.tsx handleResolve()` hardcodes `sla_state:'On Time'` on every resolution regardless of actual breach — silently corrupts SLA reporting truth. | `Oasis-Baklawa-Central/src/pages/admin/AdminSupport.tsx` `handleResolve()` | Central |
| **FL-SUP-03** | D (Support) | **P0** | Three uncoordinated predicates over `support_tickets.status`/SLA fields produce divergent "open ticket"/"SLA breached" counts across `AdminDashboard.tsx`, `AdminSupport.tsx`, `AdminExceptions.tsx` — same defect class as the previously-fixed AdminClients KPI desync (#462), recurring independently. | `AdminDashboard.tsx`, `AdminSupport.tsx`, `AdminExceptions.tsx` | Central |
| **FL-CUST-01** | A (Customer) | **P0** | `submit_customer_general_query_v1`'s idempotency-conflict handling catches `unique_violation` (23505) and treats a payload-mismatched retry as a silent duplicate-replay instead of raising a conflict — the exact failure mode the sibling migration `20260917170000` documents fixing for tickets (which now raises `P0001`), left unfixed here. Not run live to confirm the swallow occurs — flagged on read evidence. | `20260901005600_app_e2e_buyer_documents_favourites_queries.sql` vs. the fix pattern in `20260917170000_support_ticket_idempotency_v2.sql` | Core |
| **FL-GOP-01** | C (Golden Pipeline) | **P0** | `get_finance_operations_clearance_facts_v1` hardcodes the 30%/nearest-500 advance formula instead of calling `calculate_sales_order_advance_v1()`. Created one migration before the rounding-rule fix (`20260830143300`), creating a live window where the two formulas disagreed; future changes to the canonical function will not propagate here. | `20260830143000_finance_operations_clearance_authority.sql` vs `20260830143300_advance_nearest_500_rule_v2.sql` | Core |
| **FL-GOP-02** | C (Golden Pipeline) | **P1** | Central's `financeAuthorityMap.ts` references RPC `receive_submitted_b2b_dispatch_dpls_v1`, but the migration-defined DPL-receipt RPC is named `receive_finance_dpl_v1`. Unresolved: either two distinct RPCs (a feeder + the core one) or a stale map reference — needs a follow-up read to determine which. | `20260830143400_finance_dpl_receipt_authority.sql` vs `src/lib/order-authority/financeAuthorityMap.ts` | Core + Central |
| **FL-GOP-03** | C (Golden Pipeline) | **P1** | GATEKEEPER→COMPLETE tail (`record_delivery_proof_v1`, `file_commercial_complaint_v1`, `resolve_commercial_complaint_v1`) is fully governed at the DB layer (immutable, idempotent, RBAC'd, append-only) but has **zero discoverable UI caller** in either Central or the Buyer app. Staff cannot record delivery or resolve a complaint through any known screen today. Independently confirmed by both the pipeline pass and the support pass. | `20260830143800_delivery_complaint_remedy_authority.sql`; zero matches in Central or Buyer via code search | Central (UI build) |
| **FL-SUP-04** | D (Support) | **P1** | Central `ClaimModal.tsx` raises a support ticket via a raw table insert (no RPC). The trigger's non-admin branch requires the actor to resolve as a buyer company, so this path **only succeeds for literal `admin`/`super_admin`** — any other staff role (e.g. `support_executive`) raising a ticket on a customer's behalf fails with `'approved customer company required'`. | `Oasis-Baklawa-Central/src/components/ClaimModal.tsx`; trigger `support_ticket_set_customer_context()` | Central |
| **FL-SUP-05** | D (Support) | **P1** | `SalesSupportEscalationDialog.tsx` calls `submit_customer_support_ticket_v1` (the customer-raise RPC) to implement "escalate to support." Neither the RPC's post-hardening company check nor the trigger has any branch for `sales_executive` — the call **will always fail** for its intended actor. The dialog's own copy acknowledges the one-way limitation but the underlying call is unproven to succeed at all. | `src/components/sales/crm-lite/SalesSupportEscalationDialog.tsx` | Central |
| **FL-ROLE-01** | B (Roles) | **P1** | `SALES_MANAGER` has DB-recognized authority in two RLS policies (`sales_relationship_foundation.sql`, `sales_department_completion.sql`) but no entry in `STAFF_ROLE_DESTINATIONS`/`ADMIN_STAFF_ROLES` — an account with this role is rejected at login (`STAFF_MEMBERSHIP_REQUIRED`). Orphaned role: DB authority with no reachable UI. | `20260802160306_sales_relationship_foundation.sql`, `20260802160315_sales_department_completion.sql` vs `src/lib/auth-routing.ts` | Central (routing fix) |
| **FL-SUP-06** | D (Support) | **P1** | No ticket acknowledgement write path exists (`sla_first_response_at` never written anywhere); no customer communication/message-thread capability exists at all (no table, only a documented-but-unbuilt entity); no automatic customer notification is ever queued on any ticket-lifecycle event. Together these mean the journey cannot reach a genuine two-way "communication → customer update → customer-visible closure" as the mission defines it — closure today is a silent status flip the customer may never be told about outside polling the app. | Multiple — see SUPPORT_JOURNEY_MATRIX.md §5, §6, §9 | Core + Central |
| **FL-SUP-07** | D (Support) | **P1** | No SLA/escalation timeout automation exists — confirmed absent, not merely undiscovered (codebase's own commit message: "no scheduler infrastructure exists in this repo for that"). Breach detection is client-side-only, computed twice with different logic, and never escalates (no reassignment, notification, or severity bump on breach). | `20260824140000_lane1_b2_rgs_escalation_bridge.sql` commit note; `AdminSupport.tsx`, `AdminDashboard.tsx` | Core (scheduler infra does not exist repo-wide) |
| **FL-SUP-08** | D (Support) | **P1** | `routed_to_department` is a client-computed label only — no queue, notification, or RPC actually routes work to the named department. Self-documented by Central's own routing-contract doc as a known blocked prerequisite. | `src/lib/department-queues/departmentQueueRoutingContract.ts` | Core + Central |
| **FL-ROLE-02** | B (Roles) | **P2** | `/sales/3pgs-visibility` route excludes ADMIN/SUPER_ADMIN, unlike every sibling admin route in the app (`allowedRoles={["SALES_EXECUTIVE"]}` only). Inconsistent with the otherwise-uniform "ADMIN reaches everything" design. | `Oasis-Baklawa-Central/src/App.tsx`, `/sales/3pgs-visibility` route definition | Central |
| **FL-ROLE-03** | B (Roles) | **P2** | `PACKING_SUPERVISOR`'s login destination is `/admin/dispatch-mgmt`, not a dedicated packing/PNA screen — role name and landing surface mismatch. Not confirmed as functionally broken (nav may still expose the correct screen) — needs follow-up trace. | `src/lib/auth-routing.ts` | Central |
| **FL-ROLE-04** | B (Roles) | **P2** | Several production-mutation RPCs are authorized via blanket `is_internal_staff()` rather than the calling job's own department role — any internal staff role can call them directly, not just Production/Dispatch. Self-documented, accepted-scope in the migration's own header comment, not yet remediated. | `20260817150000_rgs_authority_hardening.sql` header comment | Core |
| **FL-GOP-04** | C (Golden Pipeline) | **P2** | Two unconnected complaint/delivery systems exist in parallel (`support_tickets` — weak, actually used; `commercial_complaints`/`delivery_proofs` — well-governed, entirely unused). No architectural decision found to consolidate or explicitly retire either. | Cross-reference GOLDEN_ORDER_PIPELINE_MATRIX.md §8 + SUPPORT_JOURNEY_MATRIX.md top note | Core + Central (architecture decision, then implementation) |
| **FL-SUP-09** | D (Support) | **P2** | Ticket assignment (`handleAssign`) uses hardcoded client-side SLA timer constants (2h/24h/72h) instead of server-derived policy, and writes no audit record. Admin rating also writes no audit record. | `Oasis-Baklawa-Central/src/pages/admin/AdminSupport.tsx` `handleAssign()` | Central |
| **FL-CUST-02** | A (Customer) | **P3** | No dedicated `reorder_*` RPC found anywhere — reorder, if implemented, is unverified client-side wiring with no located screen. | Searched migrations + both app repos, no hits | Buyer app (feature build) |
| **FL-ROLE-05** | B (Roles) | **P3** | FINANCE_HEAD vs FINANCE_EXEC functional differential unverified — no RLS policy found splitting authority between them beyond title. | Not independently verified this pass | — (verify, then decide) |
| **FL-ROLE-06** | B (Roles) | **P3** | Logout/session-expiry/recovery flow not traced for any role this pass. | Not opened this pass | Central |
| **FL-ROLE-07** | B (Roles) | **P3** | The two previously-known #462 defect classes (Select-in-Sheet, KPI/list desync) were confirmed fixed only in the one screen (`AdminClients.tsx`) where they were originally found, and spot-checked clean in 3 other screens (Finance, Sales, Operations) — not exhaustively scanned across all ~90 `src/pages/admin/*.tsx` files. FL-SUP-03 above proves the KPI-desync class *does* recur elsewhere (Support), so this is not a hypothetical follow-up. | Cross-reference INTERNAL_ROLE_JOURNEY_MATRIX.md | Central (scan + fix) |

## NOT-YET-CLASSIFIABLE (genuine UNPROVEN, no defect asserted)

These are explicitly **not** defects — they are gaps in this audit's coverage, listed here only so they are not silently treated as proven clean. See each journey matrix's "NOT COVERED" section for the full list. Highest-value follow-ups: RGS (production) and 3PGS (packing) migration bodies were never read line-by-line in any pass; `msg91-session-bridge` OTP mechanics are credential-gated; Central staff-side ticket screens beyond `AdminSupport.tsx` were not opened in the customer-journey pass.
